package me.ethicnology.prompt

import com.journeyapps.barcodescanner.CaptureActivity

/** Local QR surface whose manifest entry follows the device's current orientation. */
class PairingCaptureActivity : CaptureActivity()
