name: catalog
description: Local component workbench for the shared UI kit.
publish_to: none
version: 0.1.0
resolution: workspace
environment:
  sdk: ^3.12.2
dependencies:
  flutter:
    sdk: flutter
  design_system: ^0.1.0
  cupertino_icons: ^1.0.9
  widgetbook: ^3.25.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true
